import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mmasser95.cifrasSolver',
  appName: 'cifras-solver',
  webDir: 'dist'
};

export default config;
